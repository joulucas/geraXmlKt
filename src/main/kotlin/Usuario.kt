class Usuario {
//    private var nome: String? = null
//    private var idade: Int? = null
//    private var cpf: String? = null
//    private var email: String? = null
//    private var endereco: String? = null
//    private var telefone: String? = null
//    private var dataNascimento: String? = null
//    private var sexo: String? = null

    var nome: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }

    var idade: Int = 0
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var cpf: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var email: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var endereco: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var telefone: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var dataNascimento: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }
    var sexo: String = ""
        get() = field // Getter personalizado que retorna o nome em maiúsculas
        set(value) {
            field = value // Setter padrão que atribui o valor à propriedade 'field'
        }

}